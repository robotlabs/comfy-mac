```bash
cd /workspace/runpod-slim/ComfyUI && source .venv-cu128/bin/activate && pip install -q -U huggingface_hub hf_xet && export HF_XET_HIGH_PERFORMANCE=1 && HF=$(command -v hf||echo huggingface-cli) && dl(){ [ -f "$3/$(basename "$2")" ]&&{ echo "skip $(basename "$2")";return; }; mkdir -p "$3"; $HF download "$1" "$2" --local-dir .dl && mv -f ".dl/$2" "$3/$(basename "$2")"; } && dl Comfy-Org/flux1-schnell flux1-schnell.safetensors models/diffusion_models && dl comfyanonymous/flux_text_encoders clip_l.safetensors models/text_encoders && dl comfyanonymous/flux_text_encoders t5xxl_fp8_e4m3fn.safetensors models/text_encoders && dl Comfy-Org/Lumina_Image_2.0_Repackaged split_files/vae/ae.safetensors models/vae && rm -rf .dl && echo "=== DONE flux-schnell — riavvia ComfyUI ==="
```
